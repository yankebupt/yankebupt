language: ruby
addons:
  apt:
    packages:
    - libcurl4-openssl-dev
script: jekyll build
deploy:
  provider: pages
  cleanup: false
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main
